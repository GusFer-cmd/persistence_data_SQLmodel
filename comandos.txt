uv init

uv add fastapi sqlmodel uvicorn alembic

uv run alembic init alembic

uv add python-dotenv

uv add pydantic-settings

uv run alembic revision --autogenerate -m "init"
(adicionar "import sqlmdel" ) alembic > version > migration

uv run alembic upgrade head

uv run python -m app.seeds.seed_data

uv run uvicorn main:app --reload

PARA O SUPABASE - caso nao funcionar, é só executar sem o "uv run" e einstalar com o pip (o banco remoto já está povoado, não é preciso usar o python -m app.seeds.seed_data):
    uv pip install psycopg2-binary